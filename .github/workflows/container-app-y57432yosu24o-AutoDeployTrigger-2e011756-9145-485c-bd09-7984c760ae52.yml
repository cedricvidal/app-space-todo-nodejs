name: Trigger auto deployment for container-app-y57432yosu24o

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-y57432yosu24o-AutoDeployTrigger-2e011756-9145-485c-bd09-7984c760ae52.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPY57432YOSU24O_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPY57432YOSU24O_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPY57432YOSU24O_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPY57432YOSU24O_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPY57432YOSU24O_REGISTRY_PASSWORD }}
          containerAppName: container-app-y57432yosu24o
          resourceGroup: appspacetodonodejs9d31
          imageToBuild: default/container-app-y57432yosu24o:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


